let height = 10;
let base = 4;
let perimeter = 2 * (base + height);
let area = base * height;

console.log('the perimeter of a rectangle with side lengths 10 and 4 is', perimeter);
console.log('the area of a rectangle with side lengths 10 and 4 is', area);