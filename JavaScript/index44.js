let text = "HELLO"
const firstChar = text.charAt(0)
const secondChar = text.charAt(1)
const thirdChar = text.charAt(2)
const fourthChar = text.charAt(3)
const fifthChar = text.charAt(4)

const result = (fifthChar + fourthChar + thirdChar + secondChar + firstChar)
console.log("The final result is:", result.toLowerCase())